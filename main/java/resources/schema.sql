CREATE TABLE coin_chinese_table(id char(3) not null primary key,coin_type char(3),chinese_name char(3));

CREATE TABLE CURRENCY_MAPPING (eng_name CHAR(3) NOT NULL PRIMARY KEY,ch_name CHAR(2) NOT NULL);